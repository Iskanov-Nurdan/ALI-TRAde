# Кто может открывать /django-admin/.
#
# По умолчанию — все: закомментированные строки ниже ничего не ограничивают.
# Если вы заходите с постоянного адреса, раскомментируйте и укажите его —
# это надёжнее любого лимита попыток, потому что перебирать пароли будет
# просто некому.
#
# Узнать свой адрес: curl -s ifconfig.me
#
# allow 203.0.113.10;      # офис
# allow 198.51.100.0/24;   # домашняя сеть
# deny all;

# Заглушка: без директив внутри location nginx ругается на пустой include.
allow all;
